namespace = deity

country_event = {
	id = deity.1
	is_triggered_only = yes
	hide_window = yes
	
	trigger = {
		has_origin = civic_voredeity
	}

	immediate = {
			if = {
				limit = { ruler = { has_trait = trait_ruler_prey } }
				ruler = { remove_trait = trait_ruler_prey }
			}
			ruler = { add_trait = trait_ruler_voredeity }
		}   
	}
}
