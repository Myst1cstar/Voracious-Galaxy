VOREBIOLOGICAL = {
	inherit_trait_points_from = BIOLOGICAL
	inherit_traits_from = { BIOLOGICAL }
	resources = {
		category = planet_pops
		# Resource production from other sources
		inline_script = "pop_resources/pop_misc_production"
		produces = {
			trigger = {
				exists = planet
				planet = {
					has_modifier = astralnomical_interest_modifier
				}
				is_enslaved = no
			}
			physics_research = 1
		}
	}
}
VOREMACHINE = {
	inherit_trait_points_from = MACHINE
	inherit_traits_from = { MACHINE }
	robotic = yes
	resources = {
		category = planet_pops

		# Resource production from other sources
		inline_script = "pop_resources/pop_misc_production"
	}
}