gender_superiority = {
	potential = {
		is_country_type = default
		is_ai = no
		is_machine_empire = no
	}

	option = {
		name = "gender_equality"

		valid = {
			always = yes
		}

		modifier = {
			
		}
	}

	option = {
		name = "male_superiority"

		valid = {
			always = yes
		}

		on_enabled = {
			every_owned_leader = { limit = { gender = female }
				set_is_female = no
				set_name = random
			}
			every_pool_leader = { limit = { gender = female }
				set_is_female = no
				set_name = random
			}
		}

		policy_flags = {
			male_dominated
		}

		modifier = {
			
		}
	}

	option = {
		name = "female_superiority"

		valid = {
			always = yes
		}

		on_enabled = {
			every_owned_leader = { limit = { gender = male }
				set_is_female = yes
				set_name = random
			}
			every_pool_leader = { limit = { gender = male }
				set_is_female = yes
				set_name = random
			}
		}

		policy_flags = {
			female_dominated
		}
		
		modifier = {
			
		}
	}
}
